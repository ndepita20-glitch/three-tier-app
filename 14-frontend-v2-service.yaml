apiVersion: v1
kind: Service
metadata:
  name: frontend-v2-service
  namespace: three-tier-app
spec:
  selector:
    app: frontend-v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP